language: python

python:
 - "3.5"

install:
 - pip install python-coveralls
 - pip install coverage
 - pip install nose

 - pip3 install colored


script:
 - pytest
 - nosetests --with-coverage


after_success:
 - coveralls
